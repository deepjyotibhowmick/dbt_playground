models:
  - name: stg_taxizones
    description: "Taxizone data with location"
    columns:
      - name: LocationID
        description: "The primary key for this table"
        data_tests:
          - unique
          - not_null